import React from 'react';

const LazyDemo = () => (
  <div style={{ textAlign: 'center', margin: '1rem 0', color: '#888' }}>
    <strong>This is a lazily loaded component (code splitting demo).</strong>
  </div>
);

export default LazyDemo; 